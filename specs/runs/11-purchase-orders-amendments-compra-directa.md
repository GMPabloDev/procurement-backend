# RUN SPEC 11 — Purchase Orders, amendments y compra directa

> **Formato:** sdd-run/v2
> **Estado del run:** Integrado
> **Spec:** specs/11-purchase-orders-amendments-compra-directa.md
> **Revisión contractual:** 1
> **Commit de la spec:** 722419986d17ea46031cce4ce17d2c9a19df42a4
> **Blob aprobado:** 08cb2d2fe24828e4585594cc59b80cc8fbd6c792
> **Digest contractual:** 3df90fea3a48e59ec84b57a248c6d29cd5c7eae2a10b865ea44f7d69aa3f5cf9
> **Rama base:** main
> **Commit base:** 722419986d17ea46031cce4ce17d2c9a19df42a4
> **Rama de implementación:** spec-11-purchase-orders-amendments-compra-directa
> **Aislamiento Git:** Rama dedicada
> **Modo de revisión:** final
> **Iniciado:** 2026-09-18 09:40 -0500
> **Actualizado:** 2026-09-19 12:45 -0500
> **HEAD verificado:** 059f9ef50a855533d73495bb14ff30cf2e66e9ea
> **Commit de integración:** dae4f03cc2a80d05f30b57ed63ebfb1a92bdfcc5

## Commit del código probado

- `43885b199fdf68db119148f1d1268eec8aab3989` (rama `spec-11-purchase-orders-amendments-compra-directa`): contiene exactamente el árbol sobre el que se ejecutaron las suites de CP-01 y CP-03. Autorizado por el usuario; aún **no** es candidato de integración porque faltan T-05…T-09 y la revisión independiente.

## Línea base

- `dotnet build ProcureToPay.sln --nologo -v q` → 0 errores, 35 avisos preexistentes (xUnit/EF1002/EF1003 en suites de Suppliers, ReferenceCatalogs, Sourcing y Budget).
- `dotnet test --project tests/ProcureToPay.UnitTests/ProcureToPay.UnitTests.csproj --no-restore` → 293/293 correctos antes de tocar código.
- `dotnet test --project tests/ProcureToPay.IntegrationTests/ProcureToPay.IntegrationTests.csproj --no-restore --filter-class "*Sourcing*"` → 51/51 correctos (Docker/Testcontainers, migración real) tras el cambio de takeover; línea base completa no re-ejecutada.
- `dotnet test --project tests/ProcureToPay.ApiE2ETests/ProcureToPay.ApiE2ETests.csproj --no-restore` → 33/33 correctos.
- Nota: `ProcureToPayDbContextFactory` (diseño) usa autenticación integrada y no conecta en Linux; `dotnet ef migrations add` funciona sin conexión, `remove` requiere `PROCURETOPAY_DESIGN_CONNECTION` (parche local no versionado, revertido).

## Progreso por tareas

| Tarea | Estado | Evidencia | HEAD/checkpoint |
|---|---|---|---|
| T-01 | Verificada | Núcleo PO, claim y takeover por línea: dominio `src/ProcureToPay.Domain/Modules/PurchaseOrders/{PurchaseOrderCodes,PurchaseOrderEnums,PurchaseOrderModels,VendorTermsSnapshot,PurchaseOrderClaim,PurchaseOrderCanonicalizer,PurchaseOrderFingerprints,AcceptanceResponsibilityBuilder}.cs`, persistencia `src/ProcureToPay.Infrastructure/Persistence/PurchaseOrders/{PurchaseOrderPersistenceModels,PurchaseOrderModelConfiguration,PurchaseOrderSerialization,PurchaseRequestLineTakeoverService,PurchaseOrderClaimService}.cs`, migración `20260918153719_Spec11PurchaseOrderCore` (constraints, triggers append-only, expansión 1:1 de takeovers legacy), cableado de SPEC 10 al takeover por línea (`SourcingProcessService`, `SourcingAwardService`, `SourcingPrerequisiteProcessor`) y guarda de PR (`PurchaseRequestPersistenceService`: `PR_REQUEST_CONSUMED`). Pruebas: `PurchaseOrderContractTests` (22 unitarias) y `PurchaseOrderClaimIntegrationTests` (8 SQL: claim único, replay, conflicto de preimagen, cobertura parcial, release pre-issue con re-claim, recovery REOPEN/CANCEL, append-only, carrera concurrente). | commit 43885b1 · CP-01 |
| T-02 | Verificada | Acceptance Responsibilities y términos: `AcceptanceResponsibilityBuilder` (kinds exactos por purchase type, default Requested For, override con motivo, rechazo de tipo ajeno), `VendorTermsSnapshot` (`vendor-terms-snapshot/v1` desde Supplier/award/catálogo con restatement del award y 422 por término contradictorio o moneda no soportada) y `PurchaseOrderDraftService` (sucesora DRAFT con delivery/assignments, usuarios activos de la organización, CAS de versión y punteros). Pruebas: unitarias de responsabilidades/términos y `PurchaseOrderDraftIntegrationTests` (3 SQL: sucesora con assignments, usuario inactivo/ajeno rechazado, bytes publicados no reescribibles). | commit 43885b1 · CP-01 |
| T-03 | Verificada | Código implementado y compilando: `PurchaseRequestOrderingEvidenceService` (produce `purchase-request-ordering-evidence/v1` server-side desde el case completado, sus requirements/decisions y los prerequisites satisfechos; persiste y rehashea), `PurchaseOrderApprovalAdapter` (descriptor `purchase-order-approval-adapter/v1`, requirement único `PROCUREMENT_APPROVER + PROCUREMENT` con scope ORGANIZATION, acciones APPROVE/REJECT/REQUEST_CHANGES y Buyer/requester excluidos, snapshot `purchase-order-approval-snapshot/v1`, exige evidencia financiera aprobada y cobertura exacta de targets), `PurchaseOrderIssuanceService.SubmitAsync` (DRAFT→PENDING_APPROVAL con caso de Approval y CAS de versión) y `PurchaseOrderResultConsumer` (APPROVED→APPROVED con `approval_ref`, REQUEST_CHANGES→DRAFT, resto→CANCELLED; ignora eventos de versiones superseded). Verificación: `PurchaseOrderIssuanceIntegrationTests` (submit real con el adapter: caso `ISSUE_PURCHASE_ORDER` con un requirement `PROCUREMENT_APPROVER` sobre la línea exacta, estado `PENDING_APPROVAL` sin `approval_ref`, resultado `APPROVED` proyectado por el consumer, y rechazo del submit cuando el caso de la PR no está completado). | commit 43885b1 · CP-03 |
| T-04 | Verificada | Código implementado y compilando: `PurchaseOrderBudgetProducer` (resolver de reserva exact-one por target con remanente, `COMMIT` all-or-nothing vía `BudgetTransitionService` con `command_version=budget-transition-command/v1` y source id/version/digest de la PO, `REVERSE` del remanente con las mismas keys, `PurchaseOrderBudgetAttempt` durable por operación con replay) y `PurchaseOrderIssuanceService.IssueAsync` (exige APPROVED + claim vivo, emite `ISSUED` con refs de presupuesto e `issued_at`, avanza punteros de línea, proyecta `ORDERED` sin retroceso y publica outbox; recupera por key). Registro productivo en `appsettings.json` (`Approval:Workloads` y `Budget:MovementProducers` COMMIT/REVERSE `PURCHASE_ORDER`). Verificación: `PurchaseOrderIssuanceIntegrationTests` con reserva real (`REQUESTED`+`RESERVED` sobre la posición de cost center/spend category del catálogo): un `COMMITTED` por el importe exacto, `REVERSE` del remanente (`50 − 20`), balance `Committed=20/Reserved=0`, claim `ISSUED`, proyección `ORDERED` y refs `COMMIT`+`REVERSE` en la versión `ISSUED`. | commit 43885b1 · CP-03 |
| T-05 | Verificada | Amendments append-only sobre una PO emitida: `PurchaseOrderAmendmentService` (create/submit/apply/cancel con lineage único por PO, delta por línea contra el documento completo anterior, `REDUCE|CANCEL|COMMERCIAL`, delivery/responsibility sin award ni approval, successor award current validado contra sus líneas, CAS del pointer de la PO y de la raíz de amendments, `APPLYING→APPLIED`, cancelación total `CONSUMED_CANCELLED` + takeovers liberados, proyección `ORDERED` y audit), `PurchaseOrderAmendmentApprovalAdapter` (mismo adapter exact-one con subject `PURCHASE_ORDER_AMENDMENT`/`APPLY_PURCHASE_ORDER_AMENDMENT`), extensión de `PurchaseOrderApprovalAdapter` para el snapshot del amendment (digest del amendment, líneas resultantes, importe del requirement = total resultante) y proyección en `PurchaseOrderResultConsumer`. Presupuesto: `CommitAmendmentIncreaseAsync` (COMMIT solo del delta contra reservas adicionales del award sucesor + REVERSE del remanente) y `ReleaseAmendmentReductionAsync` (REVERSE del compromiso abierto a RESERVED y luego de la reserva a AVAILABLE, source exacto del padre, attempt durable). Pruebas: `PurchaseOrderAmendmentContractTests` (10 unitarias) y `PurchaseOrderAmendmentIntegrationTests` (4 SQL: delivery sin approval, reducción 20→10 con approval y reversa 10+10, cancelación total con claim consumido y takeovers liberados, aumento con award sucesor real y delta 10). Regresión dirigida: Budget 15/15, PurchaseOrders 17/17, Sourcing 51/51. | working-tree sobre 43885b1 · CP-04 |
| T-06 | Verificada | Compra directa server-side: dominio `DirectPurchaseAuthorization` (`direct-purchase-authorization/v1`, preimagen exacta de 22 propiedades, cantidad base sin publicar), fingerprints `direct-purchase-command/v1` + `authorization_fingerprint` (key/PR version/targets/case/ordering evidence/supplier/máximos/terms server-side) y reader estricto en `PurchaseOrderSerialization`. Servicio `DirectPurchaseService`: PR current aprobada, targets del manifest de la versión, `purchase-request-ordering-evidence/v1` con cobertura exacta, ruta del bundle current (exige `ALLOW_DIRECT_PURCHASE` por target y prohíbe `REQUIRE_PO|BLOCK`), prerequisites del case todos `SATISFIED`, Supplier atestiguado único y current `ACTIVE` con moneda soportada, monedas source/base compartidas y base de la organización, máximos derivados de las líneas (Σ source/base), términos `ForDirectPurchase`, assignments completos por tipo con usuarios activos, `document_evidence_refs` reales, takeover `DIRECT_PURCHASE_AUTHORIZATION` y proyección `DIRECT_PURCHASE_AUTHORIZED` sin COMMIT (la reserva queda `RESERVED`). Cancelación con `CANCELLED` append-only, release `budget-release-command/v1` + `trigger=DIRECT_PURCHASE_CANCELLED` y liberación de takeovers. Pruebas: `DirectPurchaseContractTests` (6 unitarias) y `DirectPurchaseIntegrationTests` (4 SQL: autorización sin COMMIT con takeover/proyección, replay vs preimagen distinta `409`, ruta `REQUIRE_PO` sin autorización, cancelación con release de la reserva y takeover, replay idempotente). Regresión dirigida: unitarias 332/332, PurchaseOrders 21/21, Budget 15/15. | working-tree sobre 43885b1 · CP-05 |
| T-07 | Verificada | Documentos y owner real: dominio `SupportingDocument.cs` (`procurement-supporting-document/v1` con `file_ref` sin object key/URL, `supporting-document-evidence/v1` con `counts_by_target`/`document_refs`, parámetros `{document_types,minimum_count}` fail-closed) y canonicalización de ambos documentos. `SupportingDocumentService`: stage con hash SHA-256 calculado server-side desde los bytes reales (subida a `IFileStorage` con object key determinista), confirmación append-only que sella la sucesora (un root confirmado no admite otra; la corrección crea otro root), límites (20 MiB, 100 documentos por caso), targets del manifest, URL temporal ≤15 min auditada y registro exact-one del processor con binding al owner workload resuelto. `SupportingDocumentOwnerProcessor`: reclama prerequisites `supporting-document-owner/v1`, cuenta bytes únicos (sha256+length) por target dentro de la unión de tipos admitidos, emite `SATISFIED` sólo si todos los targets alcanzan el mínimo, deja `WAITING` en ausencia, terminal sin señal en corrupción/mismatch, abandona caso terminal, attempt durable con lease 30 s/heartbeat 10 s/fencing y recovery por signal registrado. `SupportingDocumentOwnerWorker` (hosted service en API, separado de Sourcing) y `SupportingDocumentHealthCheck` (storage, registration 0/1/2, binding, backlog 60 s). Pruebas: `SupportingDocumentContractTests` (4 unitarias) y `SupportingDocumentIntegrationTests` (4 SQL: dedupe/minimum/unión con evidence reproducible y una señal, staged/tipo no admitido no satisfacen, lease obsoleto reclamado con un solo signal, caso cancelado abandona). Regresión dirigida: unitarias 336/336, PurchaseOrders 25/25. | working-tree sobre 43885b1 · CP-06 |
| T-08 | Verificada | Superficie HTTP completa (REQ-11): `PurchaseOrderController` con claim, lecturas current/historial/audit, draft, submit/issue/cancel, award recovery, amendments, Direct Purchase, documentos (stage multipart/confirm/read/download) y el agregado de ordenamiento; visibilidad por perfil (Buyer/Auditor/requester; `403` por rol, `404` fuera de scope), límite de cuerpo de comando (512 KiB) y de subida (20 MiB + margen), descarga temporal `expiresInSeconds=900` auditada y Problem Details `413`/`422`/`503 /problems/purchase-order-dependency-unavailable`. Pruebas: `A_direct_purchase_and_its_documents_are_governed_over_http` (E2E real con JWT: authorize como requester con replay, 404 para rol ajeno, lectura por requester/auditor, 404 de id desconocido, stage/confirm/read/download de documento, 409 al descargar sin confirmar, 413 a 20 MiB+1 byte, cancel con takeover liberado y cero COMMIT, 401 anónimo y readiness sin marcadores) sobre la suite API/E2E completa 34/34. | working-tree sobre 43885b1 · CP-08 |
| T-09 | Verificada | Cierre de operación: runbook `docs/purchase-order-operations.md`; agregado de proyección `PurchaseRequestOrderingQuery` (`ORDERED|DIRECT_PURCHASE_AUTHORIZED|PARTIALLY_ORDERED`, con subset/mezcla) y endpoint de lectura; health `supporting-documents` (storage, registration exact-one por adapter, binding al owner workload, backlog 60 s); ejercicio del runbook `PurchaseOrderBudgetRecoveryIntegrationTests` (crash entre el COMMIT confirmado y su checkpoint: se recupera con las mismas keys/ids, sin segundo movement, sin segunda versión y sin editar filas) y la lectura durable del intento (`LoadAttemptAsync`) que evita decidir sobre estado cacheado; fence de takeover (`PurchaseRequestLineTakeoverIntegrationTests`: colisión de servicio `409` + índice único filtrado como respaldo, liberada no bloquea); captura negativa de telemetría del módulo en la E2E (sin nombre de archivo, location, supplier, user ids ni digests de 64 hex) y rutas negativas HTTP del claim (403 de rol, award ajeno 404/422/503). Evidencia del árbol final: unitarias 336/336; integración PurchaseOrders 33/33, PurchaseRequest 16/16, Budget 16/16, Sourcing 51/51, Supplier 20/20; API/E2E 34/34. | working-tree sobre 43885b1 · CP-09 |

### CP-10 — (2026-09-19 12:10 -0500) · Revisión independiente BLOCK y correcciones R14/R16/R17

árbol probado: `working-tree sobre 47c6dec` (correcciones sin commit hasta el cierre del bloque).
  - Evidencia del árbol corregido: unitarias 336/336; integración `*PurchaseOrder*` 39/39; API/E2E 34/34; `dotnet build` 0 errores.
  - R14 corregido: la clave de storage deriva del documento server-side en su versión raíz (stage y descarga usan la misma), así que un segundo stage con el mismo file ref no puede sobrescribir bytes confirmados y la URL descarga los bytes sellados; regresión que resuelve la URL real.
  - R16 corregido: el plan de reducción (allocations, sources y keys deterministas) se persiste en `ParentsJson` antes del primer efecto, cada grupo checkpointa sus refs y el retry reejecuta el plan registrado; fault injection entre fases demuestra cero movimientos nuevos y el mismo saldo.
  - R17 corregido: `PurchaseOrderCommandGate` cierra los commands ante cualquier preflight fallido (incluida base ilegible), `PurchaseOrderPreflightWorker` reintenta cada 60 s y `PurchaseOrderCommandGateMiddleware` responde `503 /problems/purchase-order-dependency-unavailable` a los commands dejando las lecturas disponibles; regresiones de gate y de reapertura.
  - Decisión interna reversible: una base ilegible en arranque no rompe el host (los commands quedan en 503 y el worker reintenta); una inconsistencia *legible* del fence sí cierra el gate y el preflight vuelve a fallar hasta corregirse.

## Checkpoints

### CP-09 — (2026-09-18 21:05 -0500) · Bloque 4 (T-09) verificado y CA-09/CA-10 cerradas

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 336/336; integración PurchaseOrders **33/33**, PurchaseRequest 16/16, Budget 16/16, Sourcing 51/51, Supplier 20/20; API/E2E **34/34**; `dotnet build ProcureToPay.sln` → 0 errores; `git diff --check` limpio.
  - Defecto corregido: un reintento del issue dentro del mismo `DbContext` decidía sobre un `PurchaseOrderBudgetAttempt` cacheado y dejaba el intento en `COMMITTING`; ahora `LoadAttemptAsync` lee el intento durable con `AsNoTracking` y desacopla la copia obsoleta del tracker, de modo que la recuperación reutiliza `ParentsJson` (ampliado con el source del padre) sin volver a resolver la reserva consumida.
  - Decisión interna reversible: `Reservations`/`ReadReservations` del intento incluyen `parent_source_*` para poder liberar el remanente sin re-resolver el ledger; es un payload durable interno, no un documento publicado ni un digest.
  - Decisión interna reversible: el health de documentos exige exactamente una registration **de su adapter**; una registration de otro adapter no degrada (otro pipeline).
  - Pendiente inmediato: commit del código probado por el usuario y revisión independiente (`sdd-implementation-reviewer`), luego metadatos finales y `--close`.

### CP-08 — (2026-09-18 19:10 -0500) · Bloque 4 (T-08 verificado, T-09 parcial)

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 336/336; integración `*PurchaseOrder*` 29/29 (4 nuevas del agregado de proyección); API/E2E completa **34/34** (incluye la nueva `A_direct_purchase_and_its_documents_are_governed_over_http` y el recorrido cross-module de SPEC 10); `dotnet build ProcureToPay.sln` → 0 errores; `git diff --check` limpio.
  - Defectos corregidos durante el bloque: el command del Direct Purchase publicaba un `line_ref` con digest vacío (ahora el assignment lleva la identidad `line_id/line_version` y el digest lo resuelve el servidor desde la línea atestiguada); `PurchaseRequestOrderingEvidenceService.ScopeOf` lanzaba 500 con un `decision_scope` no-canónico (ahora lee el descriptor canónico y falla cerrado con `503`); `CreatedAtAction` devolvía 500 tras confirmar la mutación (sustituido por `Created` con location explícita); el cancel de Direct Purchase exigía rol Buyer/Auditor al requester propietario (ahora `404` sólo si no es propietario ni buyer).
  - Decisión interna reversible: el agregado de proyección se deriva en lectura (`PurchaseRequestOrderingQuery`) contra el manifest atestado; ninguna fila inventa una proyección y un subset reporta `PARTIALLY_ORDERED`.
  - Decisión interna reversible: el body de un comando acotado (claim/amendments) admite 512 KiB; los documentos mantienen 20 MiB por archivo con `RequestSizeLimit` de 21 MiB para cubrir el overhead multipart.
  - Pendiente inmediato: cierre de CA-09/CA-10 (regresión de expansión legacy, readiness 0/2 dedicado, captura negativa de telemetría del módulo, E2E HTTP del recorrido PO claim→issue y ejercicio automatizado del runbook).

### CP-07 — (2026-09-18 18:00 -0500) · Bloque 4 (T-08) parcial

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - `dotnet build ProcureToPay.sln` → 0 errores. No hay pruebas dirigidas de T-08 todavía: CA-09 sigue **pendiente** y el API no se declara verificado.
  - Alcance real: la superficie HTTP existe con autorización por perfil (Buyer/Auditor/requester), `404` para recursos fuera de scope, validación de tamaño de subida, mapeo de Problem Details de SPEC 11 y DTOs explícitos (sin exponer object key, URL ni el documento completo).
  - Decisión interna reversible: la lista de assignments de un replacement de amendment se hereda de la línea anterior; los cambios de responsabilidad van por `responsibility_changes` (fiel a REQ-05) y la línea de reemplazo se publica completa desde el body.
  - Pendiente inmediato: T-09 (migración/preflight/readiness final, runbook `docs/purchase-order-operations.md`, E2E cross-module con JWT/workload y captura negativa de telemetría) y las pruebas API/E2E que cierran CA-09 y CA-10.

### CP-06 — (2026-09-18 17:20 -0500) · Bloque 4 (T-07) verificado

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 336/336 (4 nuevas de supporting documents); integración `*PurchaseOrder*` 25/25 (4 nuevos de documents/worker) sobre SQL Server 2022 con Testcontainers y migración real; `dotnet build ProcureToPay.sln` → 0 errores.
  - Decisión interna reversible: el object key del blob se deriva determinísticamente de `(organization, file_id, file_version)` y nunca se publica ni se registra en logs; el documento sólo lleva `file_ref`.
  - Decisión interna reversible: la confirmación es una versión sucesora insertada (v2) porque `ProcurementSupportingDocuments` es append-only; por eso `Version > 1` en la fila ya implica confirmada y una corrección crea otro root (nuevo `DocumentId`).
  - Decisión interna reversible: `IFileStorage.GenerateTemporaryDownloadUrl` acepta un lifetime opcional; el módulo pide siempre ≤15 min y los demás módulos conservan su configuración previa.
  - Cambio de fixture: las filas de `ApprovalRequirements`/prerequisites sembradas en tests ahora usan el JSON canónico del hydrator de Approval (`SerializeAuthority`, targets con `type`), porque el signal del worker hidrata el caso completo; la evidencia de ordering sigue leyendo las mismas filas.
  - Pendiente inmediato: T-08 API/límites/Problem Details (CA-09), T-09 migración/health/runbook/E2E (CA-10).

### CP-05 — (2026-09-18 16:10 -0500) · Bloque 3 (T-06) verificado

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 332/332 (6 nuevas de Direct Purchase); integración `*PurchaseOrder*` 21/21 (4 nuevos de Direct Purchase), `*Budget*` 15/15 sobre SQL Server 2022 con Testcontainers y migración real; `dotnet build ProcureToPay.sln` → 0 errores; `git diff --check` limpio.
  - Decisión interna reversible: no se publica `base_currency` (no está en el contrato de `direct-purchase-authorization/v1`); el CLR mantiene el valor sólo para validar los términos, y el lector lo deriva del `terms_snapshot`.
  - Decisión interna reversible: el `PurchaseOrderCommands.Fingerprint` de la autorización usa una preimagen **de comando** (sólo propiedades del caller), para reconocer el replay antes de recomputar evidencia server-side; el `authorization_fingerprint` publicado sigue cubriendo la evidencia derivada.
  - Decisión interna reversible: la ruta se evalúa sobre el bundle que la submission de la PR dejó referenciado (una sola evaluación persistida por versión), rehidratado con `PolicyEvaluationBundleRehydrator` y verificado contra su `result_digest`; los tests publican el bundle con el servicio real de persistencia.
  - Decisión interna reversible: la cancelación no reescribe la proyección `DIRECT_PURCHASE_AUTHORIZED` (no hay estado inverso en la proyección); lo que desbloquea la PR es la liberación del takeover, y el guard de SPEC 06 lee el takeover, no la proyección.
  - Pendiente inmediato: T-07 documents owner + worker, T-08 API/límites, T-09 migración/health/runbook/E2E.

### CP-04 — (2026-09-18 15:05 -0500) · Bloque 3 (T-05) verificado

árbol probado: `working-tree sobre 43885b1` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 326/326 (10 nuevas de amendments); integración `*PurchaseOrder*` 17/17 (4 nuevos: amendment completo, reducción con aprobación, cancelación total, aumento con award sucesor), `*Budget*` 15/15 y `*Sourcing*` 51/51 sobre SQL Server 2022 con Testcontainers y migración real; `dotnet build ProcureToPay.sln` → 0 errores.
  - Decisión interna reversible: una PO tiene **una** lineage de amendments (la raíz es única por `PoId`); un amendment posterior de la misma PO appendea una versión nueva en vez de abrir otra raíz, y cada ciclo `DRAFT→…→APPLIED` conserva su predecesor.
  - Decisión interna reversible: el case binding del submit es una **versión sucesora** insertada (nunca un UPDATE), porque el trigger append-only prohíbe reescribir `PurchaseOrderAmendments`; el pointer de la raíz avanza por CAS.
  - Cambio en frontera SPEC 08 (modifica SPEC 08 REQ-03/REQ-08): `LoadParentAsync` calcula el remanente de un padre neto de las reversas ya posteadas contra cada hijo, sin el cual la reducción de REQ-05 no puede devolver el compromiso a AVAILABLE. No cambia schema, digests ni contrato público; la suite Budget (15/15) y Sourcing (51/51) siguen verdes.
  - Decisión interna reversible: la cancelación total conserva el line set emitido (un `purchase-order-version/v1` publica al menos una línea) y expresa la cancelación con el estado `CANCELLED` y la lineage del amendment; los pointers de línea quedan `CANCELLED`.
  - Pendiente inmediato: T-06 Direct Purchase, T-07 documents owner + worker, T-08 API/límites, T-09 migración/health/runbook/E2E.

### CP-03 — (2026-09-18 14:40 -0500) · Bloque 2 (T-03, T-04) verificado

árbol probado: `commit 43885b1` (commiteado tras la verificación; mismo contenido que el `working-tree sobre 7224199` probado).
  - Tests ejecutados sobre ese árbol: unitarias 316/316; integración `*PurchaseOrder*` 13/13 (SQL Server 2022 con Testcontainers y migración real); `dotnet build ProcureToPay.sln --no-restore` → 0 errores.
  - Defectos corregidos durante el bloque: la preimagen de `purchase-order-version/v1` serializaba `delivery` como cadena embebida en vez de objeto (rompía la reproducción del documento y el digest); `checked_at` de la evidencia de ordenamiento usaba el reloj del comando (dos producciones del mismo caso daban digests distintos) y ahora se deriva del instante persistido del caso; el adapter exigía requester≠originator cuando el Buyer es ambos (se resuelve con exclusión explícita en el requirement); el requirement de Procurement necesita `AuthorityRequirement` del tipo `Procurement` con el importe base de la orden; el `REVERSE` del remanente reutiliza el source del padre (`PURCHASE_REQUEST`) conforme a SPEC 08, lo que exige registrar el producer de la PO también para ese source.
  - Decisión interna reversible: la evidencia de ordenamiento se produce y se liga en el submit (`ordering_evidence_ref` de la versión `PENDING_APPROVAL`), y el adapter la reproduce server-side; el `material_snapshot_digest` de cada target se resuelve desde el caso completado, nunca desde la PO.
  - Brecha conocida y acotada: los resultados de Approval del subject `PURCHASE_ORDER_AMENDMENT` se registran pero no se proyectan; pertenece a T-05 (amendments), aún pendiente.
  - Pendiente inmediato: T-05 amendments, T-06 Direct Purchase, T-07 documents owner + worker, T-08 API/límites, T-09 migración/health/runbook/E2E.

### CP-02 — (2026-09-18 13:10 -0500) · Bloque 2 (T-03, T-04) parcial

árbol probado: `working-tree sobre 7224199` (sin commit).
  - Tests ejecutados sobre ese árbol: unitarias 315/315; integración completa 189/189 (SQL Server 2022 + LocalStack con Testcontainers); API/E2E 33/33; `dotnet build ProcureToPay.sln --no-restore` → 0 errores; `git diff --check` limpio.
  - Alcance real: T-03 y T-04 tienen el código completo en compilación y cableado en DI/configuración, pero **no** tienen todavía pruebas dirigidas que los certifiquen; CA-04 y CA-05 siguen pendientes. No se declara ninguno como verificado.
  - Pendiente inmediato de T-03/T-04: prueba de integración del productor de ordering evidence, del snapshot del adapter, del submit (PENDING_APPROVAL + caso de Approval), del consumer de resultados (APPROVED/CHANGES_REQUESTED/REJECTED) y de la emisión con una reserva real (COMMIT + REVERSE del remanente, recuperación del attempt y proyección `ORDERED`).
  - Brecha conocida y acotada: los resultados de Approval del subject `PURCHASE_ORDER_AMENDMENT` se registran pero no se proyectan; la proyección pertenece a T-05 (amendments), aún pendiente.
  - Decisión interna reversible: la evidencia de ordenamiento se produce en el módulo de Purchase Orders leyendo las tablas de Approval (no se añadió un servicio nuevo a SPEC 06), y el `material_snapshot_digest` de cada target se resuelve desde el case completado, nunca desde la PO.

### CP-01 — (2026-09-18 11:30 -0500) · Bloque 1 (T-01, T-02)

árbol probado: `working-tree sobre 7224199` (contenido idéntico al `commit 43885b1` para los archivos de esos bloques).
  - Tests ejecutados: unitarias 315/315 correctos (22 nuevas de PurchaseOrders); integración `*Sourcing*` 51/51, `*PurchaseRequest*` 11/11, `*PurchaseOrder*` 11/11 sobre SQL Server 2022 con Testcontainers y la migración `Spec11PurchaseOrderCore` real; API/E2E 33/33. `dotnet build ProcureToPay.sln --no-restore` → 0 errores.
  - Decisión interna reversible: `award-consumption-claim/v1` exige cobertura exacta del award (la verificación de conjunto completo se hace contra `AwardCandidate.Lines` del award, no contra la respuesta filtrada del verifier).
  - Decisión interna reversible: el replay de un claim no vuelve a consumir el award; la response verificada se persiste en `AwardConsumptionClaims.AwardSnapshotJson` y se devuelve tal cual (evita un segundo audit `SOURCING_AWARD_CONSUMED`, que ahora es idempotente por effect key).
  - Decisión interna reversible: `purchase-request-line-takeover/v1` es autoritativo para Sourcing, el processor de prerequisites y la guarda de PR; las filas request-wide quedan históricas y la migración las expande 1:1.
  - Decisión interna reversible: forma de `policy_bundle_ref` = `policy-evaluation-ref/v1` (mismo documento que publica el award) y de `budget_operation_refs` = `{operation,operation_id,operation_key,source_digest,source_id,source_version}`; el enum de estados de PO no se publica en el wire.
  - Defectos corregidos durante el bloque: `PurchaseOrderVersion` no asignaba `PoId`; SQL de la migración con paréntesis erróneos en `LOWER(CONVERT(...,2))` y schema equivocado de `PurchaseRequestVersions`; lecturas con `FromSql` no componibles (se sustituyeron por `ExecuteSqlRaw` de fencing + LINQ); el trigger append-only de `AwardConsumptionClaims` impedía su máquina de estados (ahora guarda la identidad y permite solo el avance de estado).
  - Limitación de entorno registrada: el LSP del workspace no resuelve el namespace nuevo `ProcureToPay.Domain.Modules.PurchaseOrders` (diagnósticos obsoletos); la verificación se apoya en `dotnet build`/`dotnet test`.
  - Pendiente inmediato: Bloque 2 (T-03 adapter/lifecycle de Approval, T-04 producer Budget e issue saga).

## Evidencia de aceptación

| Criterio | Estado | Evidencia | Verificador |
|---|---|---|---|
| CA-01 | Cumplido | `PurchaseOrderClaimIntegrationTests`: dos instancias sobre el mismo award producen un claim/PO único (carrera + índice único filtrado), replay estable, preimagen distinta `409`, cobertura parcial `409` sin segunda PO, cancelación pre-issue devuelve takeovers al award y permite un claim nuevo, recovery REOPEN/CANCEL sin claim previo, PR sin write-back de supplier. | suite integration PurchaseOrders |
| CA-02 | Cumplido | `PurchaseOrderContractTests` (DRAFT con delivery null/assignments vacíos, versiones append-only, `subtotal=source_gross_total` y taxes/charges/discounts=0 reproducibles, rechazo de override, digest estable ante permutación de set) y `PurchaseOrderClaimIntegrationTests`/`PurchaseOrderDraftIntegrationTests` (documento reproducible, trigger append-only, dos versiones sucesoras sin update/delete). | suite integration + unitarias |
| CA-03 | Cumplido | `PurchaseOrderContractTests` (GOOD/SERVICE/SUBSCRIPTION exigen sus kinds, default Requested For, override con motivo, tipo ajeno rechazado, términos reconstruidos con UTC/ISO 4217/decimales, contradicción y moneda no soportada `422`) y `PurchaseOrderDraftIntegrationTests` (usuario inactivo/ajeno/stale `422`). | suite integration + unitarias |
| CA-04 | Cumplido | `PurchaseOrderIssuanceIntegrationTests`: el adapter publica exactamente un requirement `PROCUREMENT_APPROVER` en stage `PROCUREMENT` con scope `ORGANIZATION`, acciones APPROVE/REJECT/REQUEST_CHANGES y Buyer excluido, sobre los targets resueltos desde el caso completado de la PR; el submit exige la autoridad financiera upstream (un caso no completado o una decisión no aprobada no presenta la orden) y no reutiliza una decisión de Sourcing. El snapshot `purchase-order-approval-snapshot/v1` liga PO/amendment, award, policy bundle, evidencia de ordenamiento, targets y términos. | suite integration PurchaseOrders |
| CA-05 | Cumplido | `PurchaseOrderIssuanceIntegrationTests`: la emisión ejecuta un `COMMIT` all-or-nothing del importe ordenado y libera por `REVERSE` el remanente reservado antes de crear la versión `ISSUED`; el `PurchaseOrderBudgetAttempt` durable registra ids/keys por operación, el resolver exige exactamente una cadena `RESERVED` abierta por target con remanente suficiente, y la proyección `ORDERED` y el outbox se escriben en la misma transacción. | suite integration PurchaseOrders |
| CA-06 | Cumplido | `PurchaseOrderAmendmentContractTests` (deltas que restan documentos completos a 12 decimales, cancelación sin replacement, commercial sin successor award `422`, delta repetido `409`, documento reproducible y estable ante permutación, sucesora que conserva refs) y `PurchaseOrderAmendmentIntegrationTests` (delivery/owner cambia por amendment sin approval; reducción 20→10 exige approval, revierte 10 a RESERVED y 10 a AVAILABLE y deja el compromiso en 10; cancelación total consume el claim y libera takeovers; aumento contra award sucesor real + reserva adicional compromete solo el delta 10). El escenario de supplier/entity/currency se rechaza en `ValidateSuccessorAwardAsync` y en `ValidateDeltas` (lado unitario/integration de aumento). | suite integration PurchaseOrders + unitarias |
| CA-07 | Cumplido | `DirectPurchaseContractTests` (documento exacto y reproducible, fingerprint que liga máximos/evidencia y es estable ante permutación, términos `DIRECT_PURCHASE` del supplier, líneas/responsabilidades cerradas) y `DirectPurchaseIntegrationTests` (el comando liga key, PR version, targets, case/ordering evidence y supplier server-side; `ALLOW_DIRECT_PURCHASE` por target crea `AUTHORIZED` por máximo derivado con takeover y proyección y sin COMMIT; replay recupera y otra preimagen da `409`; `REQUIRE_PO` no autoriza; cancel libera la reserva una vez con `DIRECT_PURCHASE_CANCELLED` y devuelve los takeovers). | suite integration PurchaseOrders + unitarias |
| CA-08 | Cumplido | `SupportingDocumentContractTests` (documento `procurement-supporting-document/v1` con sus propiedades y sin location, sucesora confirmada sellada, una ref por (target,sha256,length) y rechazo del duplicado, parámetros fail-closed) y `SupportingDocumentIntegrationTests` (bytes únicos por target y por unión de tipos admitidos; mismos bytes bajo otro root/nombre/tipo cuentan una vez; staged y tipo no admitido no señalan; lease obsoleto reclamado con un solo `SATISFIED` y evidence reproducible sin attachment; caso cancelado abandona). Health `supporting-documents` cubre storage/registration/binding/backlog (verificación E2E en T-09). | suite integration PurchaseOrders + unitarias |
| CA-09 | Cumplido | `A_direct_purchase_and_its_documents_are_governed_over_http` (E2E JWT real): roles/scope (`403` de rol, `404` fuera de scope, `401` anónimo), límites (`413` a 20 MiB+1 byte, comandos acotados a 512 KiB), descarga temporal auditada de 900 s (`409` antes de confirmar), replay de la autorización y `409` de preimagen distinta, cancelación con takeover liberado y cero `COMMIT`, rutas negativas del claim (403/404/422/503) y captura negativa de telemetría del módulo (sin nombre de archivo, location, supplier, user ids ni digests). Agregado de proyección con subset/mezcla y fence de takeover por línea (servicio + índice único filtrado) en pruebas SQL. La expansión 1:1 de takeovers legacy queda verificada por la migración `Spec11PurchaseOrderCore` y la unicidad activa por línea; no existe fixture pre-migración (registrado en desviaciones). | suite API/E2E + integración PurchaseOrders |
| CA-10 | Cumplido | `SupportingDocumentHealthIntegrationTests`: storage caído → `Degraded`, 0 registrations → `Degraded`, otra adapter no degrada, binding distinto al workload resuelto → `Degraded`, attempt con `NextAttemptAt` vencido >60 s → `Degraded(SUPPORTING_DOCUMENT_ATTEMPT_OVERDUE)`; códigos sin ids/archivos/importes/digests. `PurchaseOrderBudgetRecoveryIntegrationTests`: el crash entre el COMMIT confirmado y su checkpoint se recupera por las mismas keys con un solo movement y la misma versión emitida (ejercicio del runbook sin editar filas). Constraint de versión monotónica y triggers append-only verificados por las suites del módulo; `docs/purchase-order-operations.md` documenta recuperación, rollback y evidencia corrupta. | suite integración PurchaseOrders + runbook |

## Desviaciones y bloqueos

- Limitación de entorno (una vez): `dotnet test --project … --no-restore` devuelve intermitentemente «No se ejecutaron pruebas» (exit 5) sin ejecutar nada sobre el árbol, aunque el build es correcto; la ejecución directa del runner (`tests/<proyecto>/bin/Debug/net10.0/<Proyecto> --filter-class …`) sí ejecuta y certifica. La evidencia de CP-04..CP-09 usa el runner directo; no es un defecto del código del run.
- rondas adicionales autorizadas: 2 (ronda 3 y ronda 4 autorizada de la revisión de implementación, con autorización explícita del usuario al agotar el paquete sus dos llamadas automáticas; la ronda 4 quedó acotada a R17). La ronda 4 terminó en PASS.
- Revisión independiente en BLOCK por R14, R16 y R17 (rondas 2 y 3, ya resueltos): sus correcciones están aplicadas con regresión, pero el paquete no se marca `Lista para integrar` hasta una ronda 3 (o revisión humana explícita) que confirme el delta.
- Cobertura declarada: el recorrido feliz de los commands de PO (claim→draft→submit→approval→issue) está probado por integración SQL con los servicios reales (no por HTTP); la superficie HTTP de esos endpoints se cubre en sus rutas de autorización/error y la de Direct Purchase/documentos de punta a punta. La expansión de takeovers legacy se verifica por la migración y la unicidad activa por línea, sin fixture pre-migración.

## Verificación independiente

> **Resultado:** Sin bloqueos
> **Rondas:** 4/4
> **Triaje:** R15 resuelto (ronda 2); R14 y R16 resueltos (ronda 3); R17 resuelto (ronda 4, delta 8fae432..059f9ef) tras capturar el fail-closed esperado del preflight y probar el arranque con fence incompleto, el 503 contractual y la reapertura del gate sin reinicio. Cero hallazgos abiertos.
> **Modelo efectivo:** sdd-implementation-reviewer (openai-codex/gpt-5.6-sol, effort high)
> **Método:** Ronda 1 full de 7224199..9e11fd3 (BLOCK R14-R17); ronda 2 diferencial 9e11fd3..47c6dec (R15 resuelto); ronda 3 diferencial 47c6dec..8fae432 autorizada (R14/R16 resueltos); ronda 4 diferencial 8fae432..059f9ef autorizada y acotada a R17 (PASS). Las rondas 3 y 4 las autorizó el usuario al agotar el paquete sus dos llamadas automáticas. Constancia: `specs/reviews/11-purchase-orders-amendments-compra-directa.json`.
> **Fecha:** 2026-09-19

## Cierre

- **Commit de integración:** `dae4f03cc2a80d05f30b57ed63ebfb1a92bdfcc5` — merge de `spec-11-purchase-orders-amendments-compra-directa` sobre `main` (primer padre = `7224199`, la base registrada; la base no avanzó con trabajo ajeno entre el preflight y el merge).
- **Estrategia:** merge con historia conservada (sin squash ni rebase), por lo que `HEAD verificado` `059f9ef` es ancestro directo de `dae4f03`.
- **Equivalencia del árbol integrado:** `git diff 059f9ef..dae4f03 -- . ':!specs'` → vacío (el código integrado es byte a byte el árbol revisado); el único delta son los metadatos administrativos de `5fa441c` (spec, run y constancia).
- **Evidencia:** `specctl preflight 11` coherente (0 errores) sobre `5fa441c`; constancia `PASS` vigente con R1-R17 resueltos; suites del árbol verificado: unitarias 336/336, integración PurchaseOrders 39/39, Sourcing 51/51, Budget 16/16, PurchaseRequest 16/16, Supplier 20/20, API/E2E 35/35, build 0 errores.
- **Validaciones de cierre:** `specctl doctor` y `specctl git-check 11` ejecutados desde la base integrada (ver evidencia en la respuesta del cierre).
- **Sucesores:** SPEC 05, 06, 08 y 10 quedan `Sustituida parcialmente por SPEC 11` conservando su historia y contrato.
